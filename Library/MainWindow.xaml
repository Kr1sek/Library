<Window x:Class="Library.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ListView Name="lista_Autorzy" HorizontalAlignment="Left" Height="262" VerticalAlignment="Top" Width="332">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="30" Header="Id" DisplayMemberBinding="{Binding Path=ID}" />
                    <GridViewColumn Width="80" Header="Imie" DisplayMemberBinding="{Binding Path=FirstName}" />
                    <GridViewColumn Width="80" Header="Nazwisko" DisplayMemberBinding="{Binding Path=LastName}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
